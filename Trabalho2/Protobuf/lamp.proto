syntax="proto3";

package SmartOffice;

service Lamp{
    rpc OnOffLamp (RequestLamp) returns (ResponseLamp) {}
}

message RequestLamp{
    float Value = 1;
}
message ResponseLamp{
    string name = 1;
    bool status = 2;
}